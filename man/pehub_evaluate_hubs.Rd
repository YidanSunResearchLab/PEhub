% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{pehub_evaluate_hubs}
\alias{pehub_evaluate_hubs}
\title{Stage 3: evaluate candidate hubs for significance and reproducibility}
\usage{
pehub_evaluate_hubs(
  outdir,
  filename,
  weight_method = "bin_log_ratio_sig",
  method = "log_minmax",
  k_min = 3,
  resolution = 1,
  quantile_cutoff = 0.2,
  B_pvalue = 1000,
  B_stability = 10,
  null_mode = "hist_matched",
  pvalue_cutoff = 0.05,
  stability_cutoff = 0.5
)
}
\arguments{
\item{outdir, filename, method, weight_method}{As in \code{\link{pehub_detect_hubs}}.}

\item{k_min, resolution, quantile_cutoff}{As in \code{\link{pehub_detect_hubs}};
these must match stage 2, since hubs are re-discovered during bootstrapping.}

\item{B_pvalue}{Permutations for the distance-matched null. Default \code{1000}.}

\item{B_stability}{Bootstrap iterations for reproducibility. Default \code{10}.}

\item{null_mode}{Null construction. \code{"hist_matched"} (default) preserves
each hub's distance-bin composition.}

\item{pvalue_cutoff, stability_cutoff}{Retained for interface symmetry; the
thresholds are applied in \code{\link{pehub_export_hubs}}.}
}
\value{
Invisibly, a list with \code{hub1} (hubs annotated with
\code{hub_p_value_global}, \code{hub_p_adj_global},
\code{reproducibility_rate}, \code{OE_ratio_global}), plus the separate
\code{pvalues} and \code{stability} tables. Also writes
\code{multiple_result.<filename>.hub.<method>.<weight_method>.RData}.
}
\description{
Computes, for each candidate hub, (i) an empirical p-value against a
distance-matched null model and (ii) a reproducibility rate from bootstrap
re-discovery, then merges these statistics onto the hub table.
}
\seealso{
\code{\link{pehub_export_hubs}} for the next stage.
}
