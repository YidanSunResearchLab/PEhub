% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{pehub_prepare_interactions}
\alias{pehub_prepare_interactions}
\title{Stage 1: prepare enhancer-promoter interactions}
\usage{
pehub_prepare_interactions(
  loop_file,
  loop_file_all,
  outdir,
  tss_input,
  filename,
  promoter_window = 0
)
}
\arguments{
\item{loop_file}{Path to the significant interactions in BEDPE format.}

\item{loop_file_all}{Path to the full (background) interaction set, used to
build the distance-matched null pool.}

\item{outdir}{Output directory. Created if it does not exist.}

\item{tss_input}{A \code{GRanges} of transcription start sites, with a
\code{gene_id} metadata column. See \code{\link{build_tss_from_txdb}}.}

\item{filename}{Sample name; used as a prefix for all output files.}

\item{promoter_window}{Half-width, in bp, of the promoter window around each
TSS. \code{0} treats the TSS itself as the promoter (default).}
}
\value{
Invisibly, a list with \code{prep_hichip} (significant EP tables) and
\code{all_ep_data} (background EP tables). Also writes
\code{multiple_result.<filename>.hub.all.preprocess.RData} to \code{outdir}.
}
\description{
Reads significant and background chromatin interactions, annotates each anchor
as promoter (P) or enhancer (E) using the supplied TSS annotation, and builds
the promoter-anchored enhancer-promoter (EP) interaction tables used by all
downstream stages.
}
\seealso{
\code{\link{pehub_detect_hubs}} for the next stage.
}
