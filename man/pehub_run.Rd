% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{pehub_run}
\alias{pehub_run}
\title{Run the complete PEhub pipeline}
\usage{
pehub_run(
  loop_file,
  loop_file_all,
  outdir,
  tss_input,
  filename,
  weight_method = "bin_log_ratio_sig",
  method = "log_minmax",
  promoter_window = 0,
  k_min = 3,
  resolution = 1,
  quantile_cutoff = 0.2,
  B_pvalue = 1000,
  B_stability = 10,
  null_mode = "hist_matched",
  pvalue_cutoff = 0.05,
  stability_cutoff = 0.5,
  workers = 1
)
}
\arguments{
\item{loop_file}{Path to the significant interactions in BEDPE format.}

\item{loop_file_all}{Path to the full (background) interaction set, used to
build the distance-matched null pool.}

\item{outdir}{Output directory. Created if it does not exist.}

\item{tss_input}{A \code{GRanges} of transcription start sites, with a
\code{gene_id} metadata column. See \code{\link{build_tss_from_txdb}}.}

\item{filename}{Sample name; used as a prefix for all output files.}

\item{promoter_window}{Half-width, in bp, of the promoter window around each
TSS. \code{0} treats the TSS itself as the promoter (default).}

\item{k_min, resolution, quantile_cutoff}{As in \code{\link{pehub_detect_hubs}};
these must match stage 2, since hubs are re-discovered during bootstrapping.}

\item{B_pvalue}{Permutations for the distance-matched null. Default \code{1000}.}

\item{B_stability}{Bootstrap iterations for reproducibility. Default \code{10}.}

\item{null_mode}{Null construction. \code{"hist_matched"} (default) preserves
each hub's distance-bin composition.}

\item{pvalue_cutoff, stability_cutoff}{Retained for interface symmetry; the
thresholds are applied in \code{\link{pehub_export_hubs}}.}

\item{workers}{Number of parallel workers for the null model. Requires the
\pkg{future} and \pkg{furrr} packages; if absent, runs serially with
identical results.}

\item{use_adjusted_p}{See \code{\link{pehub_export_hubs}}.}
}
\value{
Invisibly, the list returned by \code{\link{pehub_export_hubs}}.
}
\description{
Convenience wrapper that runs all four stages in order.
}
