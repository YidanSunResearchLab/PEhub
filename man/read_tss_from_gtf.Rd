% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{read_tss_from_gtf}
\alias{read_tss_from_gtf}
\title{Read transcription start sites from a GTF file}
\usage{
read_tss_from_gtf(
  gtf_file,
  feature = "transcript",
  attributes = c("gene_id", "gene_name", "transcript_id")
)
}
\arguments{
\item{gtf_file}{Path to a GTF or GFF file. May be gzipped.}

\item{feature}{Feature type to extract TSS from. Default \code{"transcript"}.}

\item{attributes}{Character vector of GTF attributes to retain as metadata
columns. Default keeps \code{gene_id}, \code{gene_name} and
\code{transcript_id}.}
}
\value{
A \code{GRanges} of 1-bp TSS positions.
}
\description{
Parses a GTF/GFF file and returns a \code{GRanges} of transcription start
sites, in the form expected by \code{\link{preprocess_hichip}}. Uses
\pkg{rtracklayer} when available, and otherwise falls back to a lightweight
\pkg{data.table} parser with no additional dependencies.
}
\examples{
\dontrun{
tss <- read_tss_from_gtf("genes.gtf.gz")
}
}
