% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leiden.R
\name{run_leiden_for_promoter}
\alias{run_leiden_for_promoter}
\title{Run Leiden community detection for one promoter's co-membership graph}
\usage{
run_leiden_for_promoter(promoter_id, comembership, resolution = 0.5, seed = 42)
}
\arguments{
\item{promoter_id}{Character scalar, the promoter identifier.}

\item{comembership}{A square weighted adjacency matrix of enhancer co-membership.}

\item{resolution}{Numeric, the Leiden resolution parameter (default 0.5).}

\item{seed}{Integer, used for \code{set.seed()} on the R side (as in the
original implementation). The Python RNG is not seeded by default,
matching \code{leiden::leiden()}'s behaviour.}
}
\value{
A tibble with columns \code{promoter_id}, \code{hub_id},
\code{enhancers} (list column) and \code{dominance}.
}
\description{
Partitions a promoter's enhancer co-membership matrix into communities using
the Leiden algorithm (Python \code{leidenalg}), then ranks communities by
total node strength (dominance). Python is provisioned automatically via
basilisk when possible; otherwise the current reticulate Python session is
used (see the installation instructions).
}
